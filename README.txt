In order to set the password for the MySQL server, rename the PASS variable in DBController.

Design Patterns used for this project:
Observer (StockMarket, Trader)
Command (Broker)

 SQL Database contains files to initialize SQL Database and load it with stocks.